{
  "name": "@tannerntannern/budgeteer",
  "version": "1.0.0-beta.0",
  "description": "Budget your resources visually",
  "files": [
    "dist"
  ],
  "scripts": {
    "docs": "rimraf docs && typedoc --out docs ./src && touch ./docs/.nojekyll && git add docs",
    "build": "rimraf dist && tsc",
    "watch": "tsc --watch",
    "version": "yarn build"
  },
  "dependencies": {
    "kiwi.js": "^1.1.1"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "touch": "^3.1.0",
    "typedoc": "^0.14.2",
    "typescript": "3.4.5"
  },
  "repository": "https://github.com/tannerntannern/budgeteer.git",
  "author": "Tanner Nielsen",
  "license": "MIT"
}
